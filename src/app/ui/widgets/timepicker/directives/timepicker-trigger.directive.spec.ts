import { TimepickerTriggerDirective } from './timepicker-trigger.directive';

describe('TimepickerTriggerDirective', () => {
  it('should create an instance', () => {
    // const directive = new TimepickerTriggerDirective(null);
    // expect(directive).toBeTruthy();
  });
});
